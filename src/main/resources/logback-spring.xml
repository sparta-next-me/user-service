<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="APP_NAME"
                    source="spring.application.name"
                    defaultValue="unknown-service"/>

    <property name="HOST" value="${HOSTNAME:-unknown-host}"/>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://34.22.87.75:5100/loki/api/v1/push</url>
        </http>

        <!-- 핵심: label 태그를 쓰지 말고, pattern으로 labels를 한 번에 구성 -->
        <format>
            <label>
                <pattern>app=${APP_NAME},host=${HOST},level=%level</pattern>
            </label>
            <message>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </message>
            <sortByTime>true</sortByTime>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="LOKI"/>
    </root>

</configuration>
